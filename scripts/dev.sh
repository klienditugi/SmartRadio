#!/usr/bin/env bash
# Convenience wrapper. Run from the local clone directory named subwave-ai.
set -euo pipefail
echo "Use: pnpm install && pnpm dev:api / pnpm dev:worker / pnpm dev:web"
echo "Ollama stays external. See README.md."
exit 0
