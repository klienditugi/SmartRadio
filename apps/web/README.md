# apps/web

Placeholder only. Bot4 owns the full React UI (routing, operator console, request inbox, provider status, settings).

The backend foundation is in `apps/api`, `apps/worker`, and `packages/*`.
