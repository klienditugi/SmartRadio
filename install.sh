#!/usr/bin/env bash
# Minimal stub. Bot4 owns the production installer (install/update/backup/doctor).
set -euo pipefail
echo "Sub Wave AI installer stub — Bot4 will replace this with the full install.sh."
echo "Ollama is an EXTERNAL service and must never be installed or managed here."
echo "For local backend development see README.md (pnpm install, api + worker)."
exit 1
